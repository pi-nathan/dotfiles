- clean: ["~"]

- brewfile:
      - homebrew/Brewfile

- link:
      ~/.gitconfig: gitconfig
      ~/.ssh/config:
          path: ssh/config
          create: true
      ~/.zshrc: zshrc
      ~/.p10k.zsh: p10k.zsh
